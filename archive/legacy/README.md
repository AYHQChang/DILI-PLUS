# DILI-PLUS legacy archive

这里保存已退出当前 DILI 单任务流水线的历史代码。归档操作不移动任何已有数据、权重、报告或图片。

| 分类 | 来源 | 迁移原因 | 当前可运行性 |
|---|---|---|---|
| `aki/` | 旧 AKI/DILI 联合张量检查和样本打印 | 当前主任务为 DILI，且旧张量输入已不存在 | 不保证 |
| `explainability/` | `06c` 的早期副本 | 依赖已不存在的旧模型模块，与当前扰动实现重复 | 不保证 |
| `figures/` | 旧综合绘图、mock Figure 1c、Figure 6 备选版 | 数据 schema 失效、使用模拟数据或已有当前版本 | 不进入主流程 |
| `tables/` | Table 1 备份版 | 与当前 Table 1 重复 | 可供追溯，不进入主流程 |
| `training/` | 预留历史训练版本分类 | 当前未发现需迁入的额外训练副本 | 不适用 |

归档文件保留原有算法与历史路径假设，仅补充“已归档”标记；如需复现实验，应先在隔离副本中核对依赖和输入 schema。
